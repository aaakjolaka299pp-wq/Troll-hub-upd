-- Rilzz Hub | Jump To Steal Slime 
-- UI Library: Obsidian

local Players             = game:GetService("Players")
local RunService          = game:GetService("RunService")
local ReplicatedStorage   = game:GetService("ReplicatedStorage")
local UserInputService    = game:GetService("UserInputService")
local Stats               = game:GetService("Stats")
local TeleportService     = game:GetService("TeleportService")
local MarketplaceService  = game:GetService("MarketplaceService")
local HttpService         = game:GetService("HttpService")
local VirtualUser         = game:GetService("VirtualUser")
local Workspace           = game:GetService("Workspace")

local LocalPlayer         = Players.LocalPlayer

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"

local Library      = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager  = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

Library.ShowToggleFrameInKeybinds = true

-- Global Variables & State
local originalCFrame = nil
local farmSavedPosition = nil
local galaxySavedPosition = nil
local beachSavedPosition = nil

local baseWalkSpeed = 16
local sprintSpeed = 60
local flySpeed = 60
local savedPosition = nil

local flyBV, flyBG, flyConn = nil, nil, nil
local antiAfkConn = nil

local CurrentFPS = 60
local _frames = 0
local _lastTick = tick()

RunService.RenderStepped:Connect(function()
    _frames = _frames + 1
    local now = tick()
    if now - _lastTick >= 1 then
        CurrentFPS = math.floor(_frames / (now - _lastTick))
        _frames = 0
        _lastTick = now
    end
end)

local CurrentPing = 0
local function ReadPing()
    local ok, val = pcall(function()
        return math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
    end)
    if ok and val then CurrentPing = val end
    return CurrentPing
end

-- Helpers
local function GetHumanoid()
    local char = LocalPlayer.Character
    return char and char:FindFirstChildOfClass("Humanoid") or nil
end

local function GetRoot()
    local char = LocalPlayer.Character
    return char and char:FindFirstChild("HumanoidRootPart") or nil
end

local function Fmt(n)
    return string.format("%.1f", tonumber(n) or 0)
end

local threads = {}

local function startLoop(key, fn, getDelay)
    if threads[key] then task.cancel(threads[key]) threads[key] = nil end
    threads[key] = task.spawn(function()
        while Toggles[key] and Toggles[key].Value do
            pcall(fn)
            local d = getDelay and getDelay() or 0.1
            if d <= 0 then task.wait() else task.wait(d) end
        end
    end)
end

local function stopLoop(key)
    if threads[key] then task.cancel(threads[key]) threads[key] = nil end
end

local function StopFly()
    pcall(function()
        if flyConn then flyConn:Disconnect() flyConn = nil end
        if flyBV then flyBV:Destroy() flyBV = nil end
        if flyBG then flyBG:Destroy() flyBG = nil end
        local hum = GetHumanoid()
        if hum then hum.PlatformStand = false end
    end)
end

local function StartFly()
    local root = GetRoot()
    local hum = GetHumanoid()
    if not root or not hum then return end

    pcall(function()
        flyBV = Instance.new("BodyVelocity")
        flyBV.Name = "RilzzFlyVelocity"
        flyBV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        flyBV.Velocity = Vector3.zero
        flyBV.Parent = root

        flyBG = Instance.new("BodyGyro")
        flyBG.Name = "RilzzFlyGyro"
        flyBG.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
        flyBG.P = 9e4
        flyBG.CFrame = root.CFrame
        flyBG.Parent = root

        hum.PlatformStand = true

        flyConn = RunService.RenderStepped:Connect(function()
            if not Toggles.Fly or not Toggles.Fly.Value or not flyBV or not flyBG then return end
            local cam = Workspace.CurrentCamera
            local dir = Vector3.zero

            if UserInputService.KeyboardEnabled then
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0, 1, 0) end
            end

            local h = GetHumanoid()
            if h then
                if h.MoveDirection.Magnitude > 0 then dir = dir + h.MoveDirection end
                if h.Jump then dir = dir + Vector3.new(0, 1, 0) end
            end

            if dir.Magnitude > 0 then
                flyBV.Velocity = dir.Unit * (Options.FlySpeed and Options.FlySpeed.Value or flySpeed)
            else
                flyBV.Velocity = Vector3.zero
            end
            flyBG.CFrame = cam.CFrame
        end)
    end)
end

-- Window & Tabs
local Window = Library:CreateWindow({
    Title            = "Rilzz Hub | Ultra Max Speed Auto Wins",
    Footer           = "v4.0.0",
    Icon             = "zap",
    NotifySide       = "Right",
    ShowCustomCursor = true,
    AutoShow         = true,
})

local Tabs = {
    Main            = Window:AddTab("Main Features",  "zap"),
    Player          = Window:AddTab("Player Features","user"),
    Stats           = Window:AddTab("Player Info",    "activity"),
    ["UI Settings"] = Window:AddTab("UI Settings",    "settings"),
}

-- Groups Main Tab
local AutoFarmGroup = Tabs.Main:AddLeftGroupbox("Auto Farm", "cpu")
local AutoCollectGroup = Tabs.Main:AddLeftGroupbox("Auto Collect Money", "dollar-sign")
local ProgressionGroup = Tabs.Main:AddRightGroupbox("Upgrade & Progression", "trending-up")
local RebirthGroup = Tabs.Main:AddRightGroupbox("SellAll & Rebirth", "refresh-cw")

-- AUTO FARM MOON BLOCK
AutoFarmGroup:AddToggle("AutoFarmMoonBlock", {
    Text = "Auto Farm Moon Lucky Block", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoFarmMoonBlock", function()
                local root = GetRoot()
                if root then
                    if not farmSavedPosition then farmSavedPosition = root.CFrame end
                    local slimesFolder = workspace:FindFirstChild("Live") and workspace.Live:FindFirstChild("Slimes")
                    local targetBlock = slimesFolder and slimesFolder:FindFirstChild("Moon Lucky Block")
                    if targetBlock then
                        local rootPart = targetBlock:FindFirstChild("RootPart")
                        local prompt = rootPart and rootPart:FindFirstChild("StealPrompt")
                        if rootPart then
                            root.CFrame = rootPart.CFrame + Vector3.new(0, 2, 0)
                            if prompt and prompt:IsA("ProximityPrompt") then
                                prompt.MaxActivationDistance = 9e9
                                prompt.HoldDuration = 0
                                for _ = 1, 3 do fireproximityprompt(prompt) task.wait(0.05) end
                            end
                            task.wait(0.2)
                            if farmSavedPosition then root.CFrame = farmSavedPosition end
                        end
                    end
                end
            end, function() return Options.MoonBlockDelay.Value end)
        else
            stopLoop("AutoFarmMoonBlock")
            farmSavedPosition = nil
        end
    end,
})
AutoFarmGroup:AddSlider("MoonBlockDelay", { Text = "Moon Block Delay", Default = 0.5, Min = 0.1, Max = 5, Rounding = 1, Suffix = "s" })

-- AUTO FARM GALAXY BLOCK
AutoFarmGroup:AddToggle("AutoFarmGalaxyBlock", {
    Text = "Auto Farm Galaxy Lucky Block", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoFarmGalaxyBlock", function()
                local root = GetRoot()
                if root then
                    if not galaxySavedPosition then galaxySavedPosition = root.CFrame end
                    local slimesFolder = workspace:FindFirstChild("Live") and workspace.Live:FindFirstChild("Slimes")
                    local targetBlock = slimesFolder and slimesFolder:FindFirstChild("Galaxy Lucky Block")
                    if targetBlock then
                        local rootPart = targetBlock:FindFirstChild("RootPart")
                        local prompt = rootPart and rootPart:FindFirstChild("StealPrompt")
                        if rootPart then
                            root.CFrame = rootPart.CFrame + Vector3.new(0, 2, 0)
                            if prompt and prompt:IsA("ProximityPrompt") then
                                prompt.MaxActivationDistance = 9e9
                                prompt.HoldDuration = 0
                                for _ = 1, 3 do fireproximityprompt(prompt) task.wait(0.05) end
                            end
                            task.wait(0.2)
                            if galaxySavedPosition then root.CFrame = galaxySavedPosition end
                        end
                    end
                end
            end, function() return Options.GalaxyBlockDelay.Value end)
        else
            stopLoop("AutoFarmGalaxyBlock")
            galaxySavedPosition = nil
        end
    end,
})
AutoFarmGroup:AddSlider("GalaxyBlockDelay", { Text = "Galaxy Block Delay", Default = 0.5, Min = 0.1, Max = 5, Rounding = 1, Suffix = "s" })

-- AUTO FARM BEACH BLOCK
AutoFarmGroup:AddToggle("AutoFarmBeachBlock", {
    Text = "Auto Farm Beach Lucky Block", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoFarmBeachBlock", function()
                local root = GetRoot()
                if root then
                    if not beachSavedPosition then beachSavedPosition = root.CFrame end
                    local slimesFolder = workspace:FindFirstChild("Live") and workspace.Live:FindFirstChild("Slimes")
                    local targetBlock = slimesFolder and slimesFolder:FindFirstChild("Beach Lucky Block")
                    if targetBlock then
                        local rootPart = targetBlock:FindFirstChild("RootPart")
                        local prompt = rootPart and rootPart:FindFirstChild("StealPrompt")
                        if rootPart then
                            root.CFrame = rootPart.CFrame + Vector3.new(0, 2, 0)
                            if prompt and prompt:IsA("ProximityPrompt") then
                                prompt.MaxActivationDistance = 9e9
                                prompt.HoldDuration = 0
                                for _ = 1, 3 do fireproximityprompt(prompt) task.wait(0.05) end
                            end
                            task.wait(0.2)
                            if beachSavedPosition then root.CFrame = beachSavedPosition end
                        end
                    end
                end
            end, function() return Options.BeachBlockDelay.Value end)
        else
            stopLoop("AutoFarmBeachBlock")
            beachSavedPosition = nil
        end
    end,
})
AutoFarmGroup:AddSlider("BeachBlockDelay", { Text = "Beach Block Delay", Default = 0.5, Min = 0.1, Max = 5, Rounding = 1, Suffix = "s" })

-- AUTO COLLECT CASH
AutoCollectGroup:AddToggle("AutoCollectCash", {
    Text = "Auto Collect Cash", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoCollectCash", function()
                local remote = ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Collect Earnings")
                for i = 1, 100 do
                    if not Toggles.AutoCollectCash.Value then break end
                    remote:FireServer(tostring(i))
                    task.wait(0.01)
                end
            end, function() return Options.CollectCashDelay.Value end)
        else
            stopLoop("AutoCollectCash")
        end
    end,
})
AutoCollectGroup:AddSlider("CollectCashDelay", { Text = "Collect Cash Delay", Default = 0.1, Min = 0.01, Max = 2, Rounding = 2, Suffix = "s" })

-- PROGRESSION
ProgressionGroup:AddToggle("AutoUpgradeSlime", {
    Text = "Auto Upgrade Slime", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoUpgradeSlime", function()
                local remote = ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Upgrade Slime")
                for i = 1, 100 do
                    if not Toggles.AutoUpgradeSlime.Value then break end
                    remote:FireServer(tostring(i))
                    task.wait(0.01)
                end
            end, function() return Options.UpgradeSlimeDelay.Value end)
        else
            stopLoop("AutoUpgradeSlime")
        end
    end,
})
ProgressionGroup:AddSlider("UpgradeSlimeDelay", { Text = "Upgrade Slime Delay", Default = 0.1, Min = 0.01, Max = 2, Rounding = 2, Suffix = "s" })

ProgressionGroup:AddToggle("AutoUpgradeJump", {
    Text = "Auto Upgrade Jump", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoUpgradeJump", function()
                ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Buy Speed Upgrade"):FireServer(3)
            end, function() return Options.UpgradeJumpDelay.Value end)
        else
            stopLoop("AutoUpgradeJump")
        end
    end,
})
ProgressionGroup:AddSlider("UpgradeJumpDelay", { Text = "Upgrade Jump Delay", Default = 0.1, Min = 0.01, Max = 2, Rounding = 2, Suffix = "s" })

ProgressionGroup:AddToggle("AutoUpgradeCarry", {
    Text = "Auto Upgrade Carry", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoUpgradeCarry", function()
                ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Upgrade Carry Limit"):FireServer()
            end, function() return Options.UpgradeCarryDelay.Value end)
        else
            stopLoop("AutoUpgradeCarry")
        end
    end,
})
ProgressionGroup:AddSlider("UpgradeCarryDelay", { Text = "Upgrade Carry Delay", Default = 0.1, Min = 0.01, Max = 2, Rounding = 2, Suffix = "s" })

-- REBIRTH & SELL
RebirthGroup:AddToggle("AutoRebirth", {
    Text = "Auto Rebirth", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoRebirth", function()
                ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Rebirth"):FireServer()
            end, function() return Options.RebirthDelay.Value end)
        else
            stopLoop("AutoRebirth")
        end
    end,
})
RebirthGroup:AddSlider("RebirthDelay", { Text = "Rebirth Delay", Default = 0.2, Min = 0.05, Max = 5, Rounding = 2, Suffix = "s" })

RebirthGroup:AddToggle("AutoSellAll", {
    Text = "Auto Sell All", Default = false,
    Callback = function(state)
        if state then
            startLoop("AutoSellAll", function()
                ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Network"):WaitForChild("Remotes"):WaitForChild("Sell All Slimes"):FireServer()
            end, function() return Options.SellAllDelay.Value end)
        else
            stopLoop("AutoSellAll")
        end
    end,
})
RebirthGroup:AddSlider("SellAllDelay", { Text = "Sell All Delay", Default = 0.2, Min = 0.05, Max = 5, Rounding = 2, Suffix = "s" })

-- Groups Player Tab
local MovementGroup = Tabs.Player:AddLeftGroupbox("Movement", "move")
local CharacterGroup = Tabs.Player:AddLeftGroupbox("Character", "user")
local PhysicsGroup = Tabs.Player:AddRightGroupbox("Physics", "shield")
local TeleportGroup = Tabs.Player:AddRightGroupbox("Teleport", "map-pin")
local UtilityGroup = Tabs.Player:AddRightGroupbox("Utility", "wrench")

-- MOVEMENT
MovementGroup:AddSlider("WalkSpeed", {
    Text = "WalkSpeed", Default = 16, Min = 16, Max = 500, Rounding = 0,
    Callback = function(v)
        baseWalkSpeed = v
        local hum = GetHumanoid()
        if hum and not (Toggles.Sprint and Toggles.Sprint.Value) then hum.WalkSpeed = v end
    end
})

MovementGroup:AddSlider("JumpPower", {
    Text = "JumpPower", Default = 50, Min = 50, Max = 500, Rounding = 0,
    Callback = function(v)
        local hum = GetHumanoid()
        if hum then hum.UseJumpPower = true hum.JumpPower = v end
    end
})

MovementGroup:AddSlider("Gravity", {
    Text = "Gravity", Default = 196, Min = 0, Max = 300, Rounding = 0,
    Callback = function(v) Workspace.Gravity = v end
})

MovementGroup:AddToggle("Sprint", {
    Text = "Sprint", Default = false,
    Callback = function(state)
        local hum = GetHumanoid()
        if hum then hum.WalkSpeed = state and (Options.SprintSpeed and Options.SprintSpeed.Value or sprintSpeed) or baseWalkSpeed end
    end
})
MovementGroup:AddSlider("SprintSpeed", { Text = "Sprint Speed", Default = 60, Min = 20, Max = 300, Rounding = 0 })

MovementGroup:AddToggle("InfJump", { Text = "Infinite Jump", Default = false })
MovementGroup:AddToggle("InfStamina", {
    Text = "Infinite Stamina", Default = false,
    Callback = function(state)
        if state then
            startLoop("InfStamina", function()
                for _, name in ipairs({ "Stamina", "stamina", "Energy", "Sprint" }) do
                    local v = LocalPlayer:FindFirstChild(name, true)
                    if v and (v:IsA("NumberValue") or v:IsA("IntValue")) then v.Value = 100 end
                    local c = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild(name, true)
                    if c and (c:IsA("NumberValue") or c:IsA("IntValue")) then c.Value = 100 end
                end
            end)
        else
            stopLoop("InfStamina")
        end
    end
})

-- CHARACTER
CharacterGroup:AddButton({
    Text = "Reset Character",
    Func = function()
        local hum = GetHumanoid()
        if hum then hum.Health = 0 end
    end
})

CharacterGroup:AddButton({
    Text = "Heal Character",
    Func = function()
        local hum = GetHumanoid()
        if hum then hum.Health = hum.MaxHealth end
    end
})

CharacterGroup:AddToggle("FullHealth", {
    Text = "Full Health", Default = false,
    Callback = function(state)
        if state then
            startLoop("FullHealth", function()
                local hum = GetHumanoid()
                if hum then hum.Health = hum.MaxHealth end
            end)
        else
            stopLoop("FullHealth")
        end
    end
})

CharacterGroup:AddToggle("NoFallDamage", {
    Text = "No Fall Damage", Default = false,
    Callback = function(state)
        if state then
            startLoop("NoFallDamage", function()
                local hum = GetHumanoid()
                if hum then hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false) end
            end)
        else
            stopLoop("NoFallDamage")
        end
    end
})

-- PHYSICS
PhysicsGroup:AddToggle("Noclip", {
    Text = "Noclip", Default = false,
    Callback = function(state)
        if state then
            startLoop("Noclip", function()
                local char = LocalPlayer.Character
                if char then
                    for _, v in ipairs(char:GetDescendants()) do
                        if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end
                    end
                end
            end)
        else
            stopLoop("Noclip")
        end
    end
})

PhysicsGroup:AddToggle("Fly", {
    Text = "Fly", Default = false,
    Callback = function(state)
        if state then StartFly() else StopFly() end
    end
})
PhysicsGroup:AddSlider("FlySpeed", { Text = "Fly Speed", Default = 60, Min = 10, Max = 300, Rounding = 0 })

PhysicsGroup:AddToggle("PlatformStand", {
    Text = "Platform Stand", Default = false,
    Callback = function(state)
        local hum = GetHumanoid()
        if hum and not (Toggles.Fly and Toggles.Fly.Value) then hum.PlatformStand = state end
    end
})

PhysicsGroup:AddToggle("AntiRagdoll", {
    Text = "Anti Ragdoll", Default = false,
    Callback = function(state)
        if state then
            startLoop("AntiRagdoll", function()
                local hum = GetHumanoid()
                if hum then
                    if not (Toggles.Fly and Toggles.Fly.Value) and not (Toggles.PlatformStand and Toggles.PlatformStand.Value) then
                        hum.PlatformStand = false
                    end
                    hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                    hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                    hum:SetStateEnabled(Enum.HumanoidStateType.Physics, false)
                    local st = hum:GetState()
                    if st == Enum.HumanoidStateType.FallingDown or st == Enum.HumanoidStateType.Ragdoll then
                        hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                    end
                end
            end)
        else
            stopLoop("AntiRagdoll")
        end
    end
})

-- TELEPORT
TeleportGroup:AddToggle("ClickTeleport", { Text = "Click Teleport (Key: F / Touch)", Default = false })

TeleportGroup:AddButton({
    Text = "Save Position",
    Func = function()
        local root = GetRoot()
        if root then savedPosition = root.CFrame end
    end
})

TeleportGroup:AddButton({
    Text = "Teleport To Saved Position",
    Func = function()
        local root = GetRoot()
        if root and savedPosition then root.CFrame = savedPosition end
    end
})

-- UTILITY
UtilityGroup:AddToggle("AntiAFK", {
    Text = "Anti AFK", Default = false,
    Callback = function(state)
        if state then
            pcall(function()
                antiAfkConn = LocalPlayer.Idled:Connect(function()
                    pcall(function()
                        VirtualUser:CaptureController()
                        VirtualUser:ClickButton2(Vector2.new())
                    end)
                end)
            end)
        else
            pcall(function() if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end end)
        end
    end
})

UtilityGroup:AddButton({
    Text = "Rejoin Server",
    Func = function()
        pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer) end)
    end
})

UtilityGroup:AddButton({
    Text = "Server Hop",
    Func = function()
        task.spawn(function()
            local ok = pcall(function()
                local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
                local data = HttpService:JSONDecode(game:HttpGet(url))
                local list = {}
                for _, srv in ipairs(data.data or {}) do
                    if type(srv.id) == "string" and srv.id ~= game.JobId and srv.playing and srv.maxPlayers and srv.playing < srv.maxPlayers then
                        table.insert(list, srv.id)
                    end
                end
                if #list > 0 then
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, list[math.random(1, #list)], LocalPlayer)
                else
                    TeleportService:Teleport(game.PlaceId, LocalPlayer)
                end
            end)
            if not ok then pcall(function() TeleportService:Teleport(game.PlaceId, LocalPlayer) end) end
        end)
    end
})

-- STATS TAB
local StatsGroup = Tabs.Stats:AddLeftGroupbox("Stats & Performance", "activity")
local labelHealth = StatsGroup:AddLabel("Health: Memuat...")
local labelFPS    = StatsGroup:AddLabel("FPS: 0")
local labelPing   = StatsGroup:AddLabel("Ping: 0 ms")

task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            local hum = GetHumanoid()
            if hum then labelHealth:SetText("Health: " .. Fmt(hum.Health) .. "/" .. Fmt(hum.MaxHealth)) end
            labelFPS:SetText("FPS: " .. CurrentFPS)
            labelPing:SetText("Ping: " .. ReadPing() .. " ms")
        end)
    end
end)

-- UI SETTINGS TAB
local MenuGroup = Tabs["UI Settings"]:AddLeftGroupbox("Menu Settings", "wrench")

MenuGroup:AddToggle("KeybindMenuOpen", {
    Default  = Library.KeybindFrame.Visible,
    Text     = "Open Keybind Menu",
    Callback = function(Value) Library.KeybindFrame.Visible = Value end,
})

MenuGroup:AddToggle("ShowCustomCursor", {
    Text = "Custom Cursor", Default = true,
    Callback = function(Value) Library.ShowCustomCursor = Value end,
})

MenuGroup:AddDropdown("NotificationSide", {
    Values = { "Left", "Right" }, Default = "Right", Text = "Notification Side",
    Callback = function(Value) Library:SetNotifySide(Value) end,
})

MenuGroup:AddDropdown("DPIDropdown", {
    Values = { "50%", "75%", "100%", "125%", "150%", "175%", "200%" }, Default = "100%", Text = "DPI Scale",
    Callback = function(Value)
        Value = Value:gsub("%%", "")
        Library:SetDPIScale(tonumber(Value))
    end,
})

MenuGroup:AddDivider()

MenuGroup:AddLabel("Menu Keybind"):AddKeyPicker("MenuKeybind", {
    Default = "RightShift", NoUI = true, Text = "Menu keybind",
})

MenuGroup:AddButton({
    Text = "Unload Script",
    Func = function()
        for key in pairs(threads) do stopLoop(key) end
        StopFly()
        if antiAfkConn then antiAfkConn:Disconnect() end
        Library:Unload()
    end,
})

-- Global Events
UserInputService.JumpRequest:Connect(function()
    if Toggles.InfJump and Toggles.InfJump.Value then
        pcall(function()
            local hum = GetHumanoid()
            if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
        end)
    end
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe or not (Toggles.ClickTeleport and Toggles.ClickTeleport.Value) then return end
    if input.KeyCode ~= Enum.KeyCode.F and input.UserInputType ~= Enum.UserInputType.Touch then return end
    pcall(function()
        local mouse = LocalPlayer:GetMouse()
        local root  = GetRoot()
        if root and mouse and mouse.Hit then
            root.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
        end
    end)
end)

Library.ToggleKeybind = Options.MenuKeybind

ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({ "MenuKeybind" })

ThemeManager:SetFolder("RilzzHub")
SaveManager:SetFolder("RilzzHub/configs")

SaveManager:BuildConfigSection(Tabs["UI Settings"])
ThemeManager:ApplyToTab(Tabs["UI Settings"])

SaveManager:LoadAutoloadConfig()

Library:Notify({
    Title       = "Rilzz Hub",
    Description = "Script Loaded Successfully!",
    Time        = 3,
})
